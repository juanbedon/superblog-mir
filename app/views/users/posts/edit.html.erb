<% provide(:page_title, "Edit #{@post.title}") %>
<% provide(:user, 'active') %>

<h1>Editing Post</h1>

<%= render 'form', post: @post, url: users_post_url(@post) %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', users_posts_path %>
